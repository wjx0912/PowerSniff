-- file encode must be UTF8
-- 弈城协议解析

function ParseData(index, data, time)
	OutputSummary("your parse info")
	OutputText(255, 0, 0, 11, "your parse info")
	TraceText(0, 0, 255, 11, "your parse info")
	return "delete"
end
